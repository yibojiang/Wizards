var wandGlow:exSprite;
var sparker:exSprite;
var wandSparker:ParticleEmitter;
var wandTrail:TrailRenderer;
var pm:ProfileManager;

function Awake()
{
	/*
	var color:Color=Color(255,0,0,255);
	wandTrail.material.SetColor("_TintColor",color);
	
	color.a=60;
	wandSparker.renderer.material.SetColor("_TintColor",color);
	
	color.a=255;
	wandGlow.color=color;
	sparker.color=color;
	*/
}

function Update () 
{

}